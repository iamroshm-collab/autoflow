const fs=require('fs');const s=fs.readFileSync('components/inventory/pos-sales.tsx','utf8');let b=0,p=0;let inS=false,inD=false,inT=false,esc=false;for(let i=0;i<s.length;i++){const ch=s[i];if(esc){esc=false;continue;}if(ch==='\\') {esc=true;continue;}if(inS){ if(ch==="'") inS=false; continue;} if(inD){ if(ch==='"') inD=false; continue;} if(inT){ if(ch==='`') inT=false; continue;} if(ch==="'") {inS=true;continue;} if(ch==='"') {inD=true;continue;} if(ch==='`') {inT=true;continue;} if(ch==='(') p++; else if(ch===')') p--; else if(ch==='{') b++; else if(ch==='}') b--; if(p<0||b<0){console.log('Negative at pos',i,'char',ch,'line',s.slice(0,i).split('\n').length);break;} } console.log('final paren',p,'final brace',b);
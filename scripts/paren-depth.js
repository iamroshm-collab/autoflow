const fs=require('fs');const lines=fs.readFileSync('components/inventory/pos-sales.tsx','utf8').split('\n');let p=0;let inS=false,inD=false,inT=false,esc=false;for(let i=0;i<lines.length;i++){const line=lines[i];for(let j=0;j<line.length;j++){const ch=line[j];if(esc){esc=false;continue;} if(ch==='\\'){esc=true;continue;} if(inS){ if(ch==="'") inS=false; continue;} if(inD){ if(ch==='"') inD=false; continue;} if(inT){ if(ch==='`') inT=false; continue;} if(ch==="'"){inS=true;continue;} if(ch==='"'){inD=true;continue;} if(ch==='`'){inT=true;continue;} if(ch==='(') p++; else if(ch===')') p--; } if(p!==0) console.log(i+1, 'p=',p, line.trim()); } console.log('final p=',p);